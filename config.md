# Configuration

| Key | Default | Description |
|-----|---------|-------------|
| `port` | `8766` | Port the MCP SSE server listens on. Change if 8766 conflicts with another service. |

After changing the port, restart Anki and update your MCP client config URL accordingly.
